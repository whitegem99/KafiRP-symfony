{% extends 'base.admin.html.twig' %}

{% block title %}Silinen Kullanıcılar{% endblock %}
{% block pageTitle %}Silinen Kullanıcılar{% endblock %}

{% block body %}

    <div class="row">

        <!--Default Data Table Start-->
        <div class="col-12 mb-30">
            <div class="box">
                <div class="box-body">

                    <table class="table table-bordered data-table data-table-default">
                        <thead>
                        <tr>
                            <th>Kullanıcı Adı</th>
                            <th>Yetkiler</th>
                            <th>İşlemi Yapan</th>
                            <th>İşlemler</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for user in users %}
                            <tr>
                                <td>{{ user.username }}</td>
                                <td>{{ mapAccess(user.roles) }}</td>
                                <td>{{ user.actingUser.username }}</td>
                                <td>
                                    {% if user.username != 'admin' %}
                                        <a class="button button-success button-sm" href="{{ path('user_enable', {'userId': user.id}) }}">Tekrar Aktif Et</a>
                                    {% endif %}
                                </td>
                            </tr>
                        {% endfor %}

                        </tbody>

                    </table>

                </div>
            </div>
        </div>
        <!--Default Data Table End-->

    </div>

{% endblock %}
